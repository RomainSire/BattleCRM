export const queryKeys = {
  // Add query key factories as features are implemented
  // Example:
  // prospects: {
  //   all: ['prospects'] as const,
  //   list: () => [...queryKeys.prospects.all, 'list'] as const,
  //   detail: (id: string) => [...queryKeys.prospects.all, 'detail', id] as const,
  // },
}
