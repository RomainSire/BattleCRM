# Git
.git
.gitignore

# Dependencies (will be installed inside Docker)
node_modules
apps/backend/node_modules
apps/frontend/node_modules

# Build outputs
apps/backend/build
apps/frontend/dist

# Dev artifacts
db_dev
.env
.env.local
apps/frontend/.env.local

# Backend tests (not needed in Docker build; excluded to prevent tsc from compiling them)
apps/backend/tests
# Test runner entrypoint imports from tests/ — exclude it too since tests are excluded
apps/backend/bin/test.ts

# Project management
_bmad
_bmad-output
docs
