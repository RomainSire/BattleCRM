# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-06"
project: "tiny-crm"

# User Description
user_description: |
  CRM data-driven pour activité freelance dev. Prospection ESN/tech avec focus sur analytics
  et A/B testing. 3 tables principales: prospects (contacts, missions, statut), interactions
  (appels, tests, entretiens avec replay), positionnements (A/B testing CV/pitchs).
  Analytics: funnel conversion, KPI, graphes. App web auto-hébergée VPS, accès multi-devices,
  utilisateur unique, authentification sécurisée (données confidentielles).

project_type: ""
selected_track: "method"
field_type: "greenfield"

# Discovery workflows selected
brainstorm_requested: true
research_requested: true
product_brief_requested: true

workflow_path: "method-greenfield"
workflow_status:
  - phase: 1
    name: "Analysis (Optional)"
    workflows:
      - id: "brainstorm-project"
        name: "Brainstorming Session"
        status: "required"
        agent: "analyst"
        command: "/bmad:core:workflows:brainstorming"

      - id: "research"
        name: "Research"
        status: "required"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"

      - id: "product-brief"
        name: "Product Brief"
        status: "required"
        agent: "analyst"
        command: "/bmad:bmm:workflows:create-product-brief"

  - phase: 2
    name: "Planning"
    workflows:
      - id: "prd"
        name: "Product Requirements Document"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"

      - id: "create-ux-design"
        name: "UX Design"
        status: "conditional"
        note: "Determined after PRD - needed if UI exists"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"

  - phase: 3
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        name: "Architecture"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"

      - id: "create-epics-and-stories"
        name: "Epics & Stories"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"

      - id: "test-design"
        name: "Test Design"
        status: "optional"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"

      - id: "implementation-readiness"
        name: "Implementation Readiness Check"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"

  - phase: 4
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        name: "Sprint Planning"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
